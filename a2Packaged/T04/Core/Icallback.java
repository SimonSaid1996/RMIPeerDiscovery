package Core;

import java.util.ArrayList;

public interface Icallback {
    void EnumKeys(ArrayList<String> keys);   //1 function to print the callbacks, get the strings and print them
    void EnumValues( ArrayList<Integer> values);
}
